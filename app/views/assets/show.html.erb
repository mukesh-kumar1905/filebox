<% title "Asset" %>

<p>
  <strong>File:</strong>
  <%= link_to @asset.file_name, download_url(@asset) %>    
      <%= link_to "Show", @asset.uploaded_file.url %> 
      <%= link_to "Edit", edit_asset_path(@asset) %>  
      <%= link_to "Destroy", @asset, :confirm => 'Are you sure?', :method => :delete %>  
</p>

<p>
  <%= link_to "Edit", edit_asset_path(@asset) %> |
  <%= link_to "Destroy", @asset, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", assets_path %>
</p>
